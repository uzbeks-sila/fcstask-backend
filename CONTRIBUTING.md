# Как вносить вклад в FCSTask

Привет! 👋  
Ты в команде FCSTask.  


### ✅ Можно и нужно:
1. **Работать в отдельной ветке** с понятным названием.
2. **Открывать Pull Request (PR)** → ждать ревью → мержить.

### ❌ Нельзя:
- Пушить напрямую в ветку `main`
- Создавать ветки с непонятными именами (`lol`, `fix123`, `update`)

---

## 📌 Как создать ветку

Каждая задача = одна ветка.  
Формат названия: **`task/название-задачи`**

> Примеры:
> - `task/db-init`
> - `task/add-login-api`
> - `task/write-tests-for-submission`

### Шаги (делать у себя в терминале):

```bash
# 1. Переключись на main и обнови его
git checkout main
git pull origin main

# 2. Создай новую ветку (замени НАЗВАНИЕ на свою задачу)
git checkout -b task/НАЗВАНИЕ-ЗАДАЧИ

# 3. Пиши код, коммить
git add .
git commit -m "feat: краткое описание"

# 4. Отправь ветку на GitHub
git push origin task/НАЗВАНИЕ-ЗАДАЧИ
